extends Node2D

var bpm = 90

var song_pos = 0.0
var song_pos_beat = 
var sec_beat = 60.0/bpm
var last_beat = 0


# Called when the node enters the scene tree for the first time.
func _ready():
	pass
	



# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
